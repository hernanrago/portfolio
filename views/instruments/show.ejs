<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    
    <h2>Instrument Details</h2>
    <p><strong>ID:</strong> <%= instrument.id %></p>
    <p><strong>Name:</strong> <%= instrument.name %></p>
    <p><strong>Symbol:</strong> <%= instrument.symbol %></p>
    <p><strong>Type:</strong> <%= instrument.type %></p>
    <p><strong>Currency:</strong> <%= instrument.currency %></p>
    <p><strong>Status:</strong> <%= instrument.active ? 'Active' : 'Inactive' %></p>
    <% if (instrument.description) { %>
    <p><strong>Description:</strong> <%= instrument.description %></p>
    <% } %>
    
    <div>
      <a href="/instruments/<%= instrument.id %>/edit">Edit</a>
      <form method="POST" action="/instruments/<%= instrument.id %>/delete" style="display: inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this instrument?')">Delete</button>
      </form>
      <a href="/instruments">Back to Instruments</a>
    </div>
  </body>
</html>