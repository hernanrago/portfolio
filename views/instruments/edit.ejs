<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style>
      .form-group { margin-bottom: 15px; }
      label { display: block; margin-bottom: 5px; font-weight: bold; }
      input, select, textarea { width: 100%; padding: 8px; border: 1px solid #ddd; border-radius: 4px; }
      textarea { resize: vertical; height: 80px; }
      .btn { padding: 10px 20px; margin: 5px; text-decoration: none; border: none; border-radius: 4px; cursor: pointer; }
      .btn-primary { background-color: #007bff; color: white; }
      .btn-secondary { background-color: #6c757d; color: white; }
      .form-container { max-width: 600px; margin: 0 auto; }
    </style>
  </head>
  <body>
    <div class="form-container">
      <h1><%= title %></h1>
      
      <form method="POST" action="/instruments/<%= instrument.id %>">
        <div class="form-group">
          <label for="name">Name:</label>
          <input type="text" id="name" name="name" value="<%= instrument.name %>" required>
        </div>
        
        <div class="form-group">
          <label for="symbol">Symbol:</label>
          <input type="text" id="symbol" name="symbol" value="<%= instrument.symbol %>" required>
        </div>
        
        <div class="form-group">
          <label for="type">Type:</label>
          <select id="type" name="type" required>
            <option value="">Select type...</option>
            <option value="Stock" <%= instrument.type === 'Stock' ? 'selected' : '' %>>Stock</option>
            <option value="Bond" <%= instrument.type === 'Bond' ? 'selected' : '' %>>Bond</option>
            <option value="ETF" <%= instrument.type === 'ETF' ? 'selected' : '' %>>ETF</option>
            <option value="Cryptocurrency" <%= instrument.type === 'Cryptocurrency' ? 'selected' : '' %>>Cryptocurrency</option>
            <option value="Commodity" <%= instrument.type === 'Commodity' ? 'selected' : '' %>>Commodity</option>
            <option value="Other" <%= instrument.type === 'Other' ? 'selected' : '' %>>Other</option>
          </select>
        </div>
        
        <div class="form-group">
          <label for="currency">Currency:</label>
          <select id="currency" name="currency" required>
            <option value="USD" <%= instrument.currency === 'USD' ? 'selected' : '' %>>USD</option>
            <option value="ARS" <%= instrument.currency === 'ARS' ? 'selected' : '' %>>ARS</option>
            <option value="EUR" <%= instrument.currency === 'EUR' ? 'selected' : '' %>>EUR</option>
            <option value="GBP" <%= instrument.currency === 'GBP' ? 'selected' : '' %>>GBP</option>
            <option value="JPY" <%= instrument.currency === 'JPY' ? 'selected' : '' %>>JPY</option>
          </select>
        </div>
        
        <div class="form-group">
          <label for="description">Description:</label>
          <textarea id="description" name="description"><%= instrument.description %></textarea>
        </div>
        
        <div class="form-group">
          <label for="active">Active:</label>
          <input type="checkbox" id="active" name="active" <%= instrument.active ? 'checked' : '' %>>
        </div>
        
        <div class="form-group">
          <button type="submit" class="btn btn-primary">Update Instrument</button>
          <a href="/instruments/<%= instrument.id %>" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </body>
</html>