<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    
    <h2>Basic Information</h2>
    <p><strong>ID:</strong> <%= investment.id %></p>
    <p><strong>Date:</strong> <%= investment.date %></p>
    <p><strong>Broker:</strong> <%= investment.broker || 'N/A' %></p>
    <p><strong>Destination:</strong> <%= investment.destination || 'N/A' %></p>
    <p><strong>Instrument:</strong> <%= investment.instrument || 'N/A' %></p>
    <p><strong>Nominals:</strong> <%= investment.nominals %></p>
    <p><strong>Currency:</strong> <%= investment.currency || 'N/A' %></p>
    <p><strong>Total Purchase:</strong> <%= investment.total_purchase ? investment.total_purchase.toFixed(2) : '0.00' %></p>
    <p><strong>Purchase Date Dollar:</strong> <%= investment.purchase_date_dollar ? investment.purchase_date_dollar.toFixed(2) : '0.00' %></p>
    
    <div>
      <a href="/investments/<%= investment.id %>/edit">Edit</a>
      <form method="POST" action="/investments/<%= investment.id %>/delete" style="display: inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this investment?')">Delete</button>
      </form>
      <a href="/investments">Back to Investments</a>
    </div>
  </body>
</html>