<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    
    <h2>Basic Information</h2>
    <p><strong>ID:</strong> <%= investment.id %></p>
    <p><strong>Date:</strong> <%= investment.date %></p>
    <p><strong>Broker:</strong> <%= investment.broker %></p>
    <p><strong>Destination:</strong> <%= investment.destination %></p>
    <p><strong>Instrument:</strong> 
      <% const instrument = investment.getInstrument(); %>
      <%= instrument ? `${instrument.symbol} - ${instrument.name}` : 'N/A' %>
    </p>
    <p><strong>Sold:</strong> <%= investment.sold ? 'Yes' : 'No' %></p>
    
    <h2>Investment Details</h2>
    <p><strong>Nominals:</strong> <%= investment.nominals %></p>
    <p><strong>Purchase Price:</strong> $<%= investment.purchase_price.toFixed(2) %></p>
    <p><strong>Current Price:</strong> $<%= investment.current_price.toFixed(2) %></p>
    <p><strong>Dollar MEP Purchase Date:</strong> <%= investment.dollar_MEP_purchase_date.toFixed(2) %></p>
    
    <h2>ARS Performance</h2>
    <p><strong>Total Purchase ARS:</strong> $<%= investment.total_purchase_ARS.toFixed(2) %></p>
    <p><strong>Total Current ARS:</strong> $<%= investment.total_current_ARS.toFixed(2) %></p>
    <p><strong>Return ARS:</strong> $<%= investment.return_ARS.toFixed(2) %></p>
    <p><strong>Return Percentage ARS:</strong> <%= investment.return_percentage_ARS.toFixed(2) %>%</p>
    <p><strong>TNA ARS:</strong> <%= investment.tna_ARS.toFixed(2) %>%</p>
    
    <h2>USD Performance</h2>
    <p><strong>Total Purchase USD:</strong> $<%= investment.total_purchase_USD.toFixed(2) %></p>
    <p><strong>Total Current USD:</strong> $<%= investment.total_current_USD.toFixed(2) %></p>
    <p><strong>Return USD:</strong> $<%= investment.return_USD.toFixed(2) %></p>
    <p><strong>Return Percentage USD:</strong> <%= investment.return_percentage_USD.toFixed(2) %>%</p>
    <p><strong>TNA USD:</strong> <%= investment.tna_USD.toFixed(2) %>%</p>
    
    <h2>Summary</h2>
    <p><strong>Average Returns:</strong> <%= investment.average_returns.toFixed(2) %>%</p>
    
    <div>
      <a href="/investments/<%= investment.id %>/edit">Edit</a>
      <form method="POST" action="/investments/<%= investment.id %>/delete" style="display: inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this investment?')">Delete</button>
      </form>
      <a href="/investments">Back to Investments</a>
    </div>
  </body>
</html>